<?xml version="1.0" encoding="utf-8"?>
<com.sokolua.manager.ui.screens.settings.SettingsView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:isScrollContainer="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">


            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/auto_sync_switch"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginLeft="@dimen/settings_padding"
                android:layout_marginRight="@dimen/settings_padding"
                android:hint="@string/settings_auto_sync" />

            <include layout="@layout/login_form" />

        </LinearLayout>
    </ScrollView>
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_app_version"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        tools:text = "ver.: 2.0"
        android:layout_margin="24dp"
        android:textSize="18sp"
        android:textStyle="bold|italic"
        android:textColor="@color/color_gray"
        />
</com.sokolua.manager.ui.screens.settings.SettingsView>